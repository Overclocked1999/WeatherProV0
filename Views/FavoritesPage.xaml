<Page
    x:Class="WeatherPro.Views.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <AutoSuggestBox
            Grid.Row="0"
            PlaceholderText="Search any city worldwide..."
            Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}"
            ItemsSource="{x:Bind ViewModel.SearchResults, Mode=OneWay}"
            QuerySubmitted="SearchBox_QuerySubmitted" />

        <TextBlock Grid.Row="1" Text="Favorites" Style="{ThemeResource SubtitleTextBlockStyle}" />

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Favorites, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="8" Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                Content="{Binding DisplayName}" Click="FavoriteItem_Click" Tag="{Binding}" />
                        <Button Grid.Column="1" Content="Remove" Click="RemoveFavorite_Click" Tag="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
